using UnityEngine;

public class VisualTileHandler : MonoBehaviour
{
    public static VisualTileHandler instance;

    private void Awake()
    {
        instance = this;
    }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
